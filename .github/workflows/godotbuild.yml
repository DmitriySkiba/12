name: Build Godot Project

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Godot
      uses: lihop/godot-setup@v2.1.3
      with:
        version: 4.2.1-stable
        mono: true

    - name: Get tree
      run: | 
        tree

    - name: Build with Godot
      run: |
        /home/runner/work/12/12/v4.2.1-stable/Godot_v4.2.1-stable_mono_linux_x86_64/Godot_v4.2.1-stable_mono_linux_x86_64-wrapper --export-release "windows" ./Fight-Jump.exe
